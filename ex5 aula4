#include <stdio.h>
#include <stdlib.h>

int main() {
    int num;

    for (num = 1; num <= 20; num++) {
        if (num % 2 == 0) {
            continue; 
        }
        if (num > 15) {
            break; 
        }
        printf("%d\n", num); 
    }

    system("pause");
    return 0;
}
